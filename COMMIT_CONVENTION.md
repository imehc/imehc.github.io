# Commit Message 规范

本项目使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范来管理提交信息。

## 🚀 快速开始（推荐）

### 交互式提交（最简单）

使用交互式命令，无需记忆格式：

```bash
# 1. 先暂存你要提交的文件
git add .

# 2. 运行交互式提交命令
pnpm commit

# 3. 按照提示选择和填写：
#    - 选择提交类型（feat, fix, docs 等）
#    - 填写影响范围（可选）
#    - 填写简短描述
#    - 填写详细描述（可选）
#    - 是否有破坏性更新（可选）
```

**示例流程：**
```bash
$ pnpm commit

? 选择你要提交的类型 : (Use arrow keys)
❯ feat:     ✨  新增功能 | A new feature
  fix:      🐛  修复缺陷 | A bug fix
  docs:     📝  文档更新 | Documentation only changes
  style:    💄  代码格式 | Changes that do not affect the meaning of the code
  refactor: ♻️  代码重构 | A code change that neither fixes a bug nor adds a feature
  perf:     ⚡️  性能提升 | A code change that improves performance
  test:     ✅  测试相关 | Adding missing tests or correcting existing tests
(Move up and down to reveal more choices)

? 选择一个提交范围（可选）:
❯ 跳过
  自定义

? 填写简短精炼的变更描述 :
  添加用户登录功能

? 填写更加详细的变更描述（可选）。使用 "|" 换行 :
  支持用户名密码登录 | 支持记住登录状态 | 添加登录状态持久化

? 列举非兼容性重大的变更（可选）。使用 "|" 换行 :
  (按回车跳过)

? 列举关联issue (可选) 例如: #31, #I3244 :
  (按回车跳过)

? 是否提交或修改commit ? (Yneh)
❯ Yes
  No
  Edit
  Help

✔ 提交成功！
```

## 🎯 为什么需要规范？

- ✅ 自动生成 CHANGELOG
- ✅ 自动计算版本号（semantic-release）
- ✅ 提高代码审查效率
- ✅ 更好的项目历史记录

## 📝 提交格式

### 方式一：交互式提交（推荐）
```bash
git add .
pnpm commit
```

### 方式二：手动提交
如果你熟悉规范，也可以直接使用 git commit：

```bash
git commit -m "feat: 添加新功能"
git commit -m "fix: 修复问题"
```

### 提交格式说明

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 必需部分
- **type**: 提交类型（必需）
- **subject**: 简短描述（必需，至少 4 个字符，最多 100 个字符）

### 可选部分
- **scope**: 影响范围（可选）
- **body**: 详细描述（可选）
- **footer**: 备注，通常是 BREAKING CHANGE 或关闭的 issue（可选）

## 📋 Type 类型说明

| Type | 说明 | 版本影响 |
|------|------|----------|
| `feat` | 新功能 | ⬆️ Minor |
| `fix` | 修复 Bug | ⬆️ Patch |
| `perf` | 性能优化 | ⬆️ Patch |
| `docs` | 文档变更 | - |
| `style` | 代码格式（不影响代码运行） | - |
| `refactor` | 重构（既不是新功能，也不是修复 bug） | - |
| `test` | 增加测试 | - |
| `chore` | 构建过程或辅助工具的变动 | - |
| `revert` | 回退 | - |
| `build` | 打包相关 | - |
| `ci` | CI 配置相关 | - |

## ✅ 正确示例

### 简单提交
```bash
git commit -m "feat: 添加用户登录功能"
git commit -m "fix: 修复登录按钮点击无效的问题"
git commit -m "docs: 更新 README 文档"
git commit -m "chore: 更新依赖版本"
```

### 带作用域
```bash
git commit -m "feat(auth): 添加双因素认证"
git commit -m "fix(api): 修复用户接口返回数据错误"
git commit -m "style(button): 统一按钮样式"
```

### 详细提交
```bash
git commit -m "feat: 添加数据导出功能

- 支持导出为 CSV 格式
- 支持导出为 Excel 格式
- 添加导出进度显示

Closes #123"
```

### 破坏性更新
```bash
git commit -m "feat!: 重构用户 API

BREAKING CHANGE: 用户 API 路径从 /api/v1/user 改为 /api/v2/users"
```

## ❌ 错误示例

```bash
# ❌ 没有 type
git commit -m "添加登录功能"

# ❌ subject 太短（少于 4 个字符）
git commit -m "feat: fix"

# ❌ 不符合规范的 type
git commit -m "update: 更新代码"

# ❌ 缺少冒号
git commit -m "feat 添加功能"

# ❌ 冒号后没有空格
git commit -m "feat:添加功能"
```

## 🛠️ 提交被拒绝怎么办？

如果你的提交信息不符合规范，会看到错误提示：

```bash
⧗   input: 添加登录功能
✖   subject may not be empty [subject-empty]
✖   type may not be empty [type-empty]
```

**解决方法：**
```bash
# 修改上一次提交信息
git commit --amend -m "feat: 添加登录功能"
```

## 💡 快速提示

### 常用命令
```bash
# 快速修复
git commit -m "fix: 修复问题描述"

# 快速新增功能
git commit -m "feat: 新功能描述"

# 文档更新（不触发版本更新）
git commit -m "docs: 文档更新内容"

# 依赖更新（不触发版本更新）
git commit -m "chore: 更新依赖"
```

### 选择合适的 type
- 🤔 **添加了新的功能或能力？** → `feat`
- 🐛 **修复了一个 bug？** → `fix`
- ⚡ **提升了性能？** → `perf`
- 📝 **只改了文档？** → `docs`
- 🔨 **重构了代码但没改功能？** → `refactor`
- 💄 **只改了格式和样式？** → `style`
- 📦 **改了构建配置？** → `build` 或 `chore`

## 🔧 本地测试

```bash
# 测试 commitlint 配置
echo "feat: 测试提交信息" | npx commitlint

# 应该看到：✔   found 0 problems, 0 warnings
```

## 📚 更多信息

- [Conventional Commits 官方文档](https://www.conventionalcommits.org/)
- [Commitlint 文档](https://commitlint.js.org/)
- [Semantic Release 文档](https://semantic-release.gitbook.io/)

---

**记住：好的提交信息让团队协作更轻松！** 🚀
